<h1> My Bookings </h1>


<div>
  <h2>Booking List</h2>
</div>
<div class="container">
  <ul>
    <li><% @bookings.each do |book| %></li>
    <li><%= book.id %></li>
    <li><%= book.car_id %></li>
    <li><%= book.fecha_inicio %></li>
    <li><%= book.fecha_fin %></li>
    <%= link_to "Anular Book", booking_path(book), method: :delete  %>


<p> Usted tiene agendados en las siguientes fechas:</p>
<br>
<% @bookings.each do |booking| %>
<ul>

<%# #Como agregar datos de car? %>
<%= booking.car.modelo %>
<%= booking.car.capacidad %>
<p><li><%=booking.fecha_inicio%></li><%=booking.fecha_fin%></li> </p>
</ul>
<br>
<% end %>
<%= link_to "Volver al menu principal", cars_path, class: "btn btn-primary" %>