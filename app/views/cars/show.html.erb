
  <div class="card-product">
    <%= cl_image_tag @car.photo.key%>
    <div class="card-product-infos">
      <h3><%= @car.modelo.capitalize %></h3>
      <p><%= @car.anio %></p>
      <p><%= @car.precio %></p>
      <p><%= @car.ubicacion %></p>
      <p><%= @car.detalles %></p>
      <p><%= @car.capacidad %></p>
    </div>
  </div>
<div class="container">
    <ul class="list-group" id="listaBookings">
      <% @car.bookings.each do |booking| %>
        <li class="list-group-item"><%= booking.fecha_inicio %></li>
        <li class="list-group-item"><%= booking.fecha_fin %></li>
        <%= link_to "Anular reserva" , booking_path(booking), class:"btn btn-outline-primary", method: :delete %></p>

        <br>
      <% end %>
  <br>
</div>
<%= link_to 'Volver', cars_path, class: "btn btn-primary"%>
<%= link_to 'Alquilar este auto', new_car_booking_path(@car), class: "btn btn-primary"%>
